(function () {
    'use strict';

    angular.module('app.pm')
        .controller('managePMCtrl', ['$scope', 'dropdownlistService', managePMCtrl])
        .controller('addPMCtrl', ['$scope', addPMCtrl])

        function managePMCtrl($scope, dropdownlistService){
            
        }

        function addPMCtrl($scope){
            
        }
})();